# Robots.txt for نوبيان | Nubian Store
# https://nubian-sd.store

# Allow all crawlers by default (everything is allowed unless disallowed)
User-agent: *

# Block specific paths that shouldn't be indexed
Disallow: /api/
Disallow: /buseniss/
Disallow: /sign-in
Disallow: /sign-up

# Crawl-delay for respectful crawling
Crawl-delay: 1

# Sitemaps
Sitemap: https://nubian-sd.store/sitemap.xml

# Explicit rules for Google (most important for indexing)
User-agent: Googlebot
Disallow: /api/
Disallow: /buseniss/
Disallow: /sign-in
Disallow: /sign-up

# Other major search engines
User-agent: Bingbot
Disallow: /api/
Disallow: /buseniss/
Disallow: /sign-in
Disallow: /sign-up
