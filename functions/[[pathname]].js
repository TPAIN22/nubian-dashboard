// Cloudflare Pages Functions entry point for OpenNext
// This will be replaced during build with the actual OpenNext worker
export async function onRequest(context) {
  // This is a placeholder - the actual implementation will be copied from .open-next/worker.js
  return new Response("Building...", { status: 503 });
}